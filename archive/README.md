# V1 / V2 历史归档

这些文件用于审计与对话追溯，不是有效设计交付。V3 已替换仓库默认入口和主报告。

- `v1/`：原根目录 V1 程序、`results/`、结构化报告和 HTML 快照。
- `v2/`：原 V2 程序、`results_v2/`、测试和 SQL 数据血缘。
- [V3 报告](../results_v3/REPORT.md)是有效的结果入口。

V2 的 X–VREF 补偿连接，在源极重定位及 DATA 写入后会丢失部分 VTH 补偿量。理想情况下 `∂(VGS−VTH)/∂VTH = −κ`，因此旧版“全部硬约束通过”结论撤回。V1 的早期代理结果也不得当成实际工艺验证。

历史源码与数据保留，不将旧筛选数字重写成 V3。重跑旧算法仅用于复现旧结果，不能恢复其有效性。旧版完整目录布局可在迁移前提交 `d287f8bd098fdfb52da86fb3f97c7fdfd5d3001b` 查看。

对话中的旧链接映射：`results/` 改查 `archive/v1/results/`；`results_v2/` 改查 `archive/v2/results_v2/`；旧 HTML 报告位于 `archive/v1/`。对话正文不因这些历史路径迁移而重写。
